#include<bits/stdc++.h>
using namespace std;
int main ()
{
    int x;
    cin>>x;
    // cin.ignore();
    getchar();
    cout<<x<<endl;
    string s;
    // cin.getline(s,100); // if we want to take input form char data type we can use it.
    getline(cin,s);
    cout<<s<<endl;
    cout<<s.size()<<endl;
    return 0;
}