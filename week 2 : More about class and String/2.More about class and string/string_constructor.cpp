#include<bits/stdc++.h>
using namespace std;
int main ()
{
    // string s = "That was not my fault";
    string s("That was her fault",2);//if we do it we will get the the value of 0-4 index;
    string a ="Is it good man";
    string c(a,4); // we will get the value after 4 index here;

    cout<<s<<endl;
    // cout<<c;
    string sr (26,'x');

    // cout<<sr<<endl;
     

    cout<<sr.size();
    return 0;
}